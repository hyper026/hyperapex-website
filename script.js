const menuToggle=document.querySelector('.menu-toggle');const navLinks=document.querySelector('.nav-links');menuToggle.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');menuToggle.setAttribute('aria-expanded',open)});document.querySelectorAll('.nav-links a').forEach(link=>link.addEventListener('click',()=>{navLinks.classList.remove('open');menuToggle.setAttribute('aria-expanded','false')}));document.getElementById('year').textContent=new Date().getFullYear();const form=document.getElementById('contactForm'),status=document.getElementById('formStatus');form.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form);const subject=encodeURIComponent(`Website enquiry - ${d.get('service')}`);const body=encodeURIComponent(`Dear Hyperapex Consultancy Firm Limited,\n\nI would like to make an enquiry.\n\nName: ${d.get('name')}\nCompany / Organization: ${d.get('company')||'N/A'}\nEmail: ${d.get('email')}\nPhone: ${d.get('phone')}\nService Required: ${d.get('service')}\n\nMessage:\n${d.get('message')}\n\nKind regards,\n${d.get('name')}`);window.location.href=`mailto:hyperapexconsult@gmail.com?subject=${subject}&body=${body}`;status.textContent='Your email application should now open with the enquiry prepared.'});